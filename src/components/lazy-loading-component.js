import React from 'react';

const LazyLoadComponent = (props) => {
    return (
        <div>
            <p>Lazy Loading Component Added !!!</p>
        </div>
    )
}

export default LazyLoadComponent;
